const assert = require("chai").assert;
const sinon = require("sinon");
const User = require("../../model/User");

describe("User.js file", () => {
  context("constructor", () => {
    it("should construct NAME and LASTNAME as null", () => {
      
    });
  });
});